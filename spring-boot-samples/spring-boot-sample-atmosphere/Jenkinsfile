pipeline {
    agent any
    
    environment { 
        PROJECT_PATH = 'spring-boot-samples/spring-boot-sample-atmosphere'
    }
    
    stages {
        stage('Preparation') {
            environment { 
                REPO_BRANCH = 'develop'
                REPO_URL = 'https://github.com/davsanssg/jenkins2-course-spring-boot.git'
            }
            steps {
                echo 'Doing nothing in preparation..'
            }
        }
        
        stage('Build') {
            agent {
                docker {
                    image 'maven:3.5-jdk-8'
                    args '-e _JAVA_OPTIONS=-Djdk.net.URLClassPath.disableClassPathURLCheck=true'
                }
            }       
            steps{
                dir(PROJECT_PATH){
                    sh 'printenv'
                    echo 'Starting Build stage...'
                    echo 'Compiling, testing and building...'
                    sh label: '', script: 'mvn clean package'
                }          
            }
        }
        
        stage('Results'){
            steps{
                dir(PROJECT_PATH){
                    echo 'Starting Results stage...'
                    junit 'target/surefire-reports/TEST-*.xml'
                    archiveArtifacts 'target/*.jar'
                }      
            }
        }
        
    }

    post { 
        always {
            echo 'Cleaning workspace...'
            cleanWs()
        }
    }
}